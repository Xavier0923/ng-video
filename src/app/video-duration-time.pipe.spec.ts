import { VideoDurationTimePipe } from './video-duration-time.pipe';

describe('VideoDurationTimePipe', () => {
  it('create an instance', () => {
    const pipe = new VideoDurationTimePipe();
    expect(pipe).toBeTruthy();
  });
});
